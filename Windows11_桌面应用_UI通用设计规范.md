# Windows 桌面应用 UI 通用设计规范

> 适用场景：Windows 11 桌面应用  
> 设计方向：Fluent 风格 / 简洁 / 紧凑 / 直观 / 高可读性  
> 文档定位：通用 UI 设计、布局、表单、信息展示、交互与边界处理规范

---

# 一、设计原则

## 1. 核心原则

桌面应用界面遵循以下原则：

1. **清晰优先**：用户应快速理解页面目的、当前状态和可执行操作。
2. **层级明确**：通过字号、字重、间距、位置和视觉强调建立信息层级。
3. **紧凑但不拥挤**：降低无效留白，而不是缩小文字或控件。
4. **状态可见**：系统状态、操作结果和异常情况必须有明确反馈。
5. **一致性**：相同功能使用相同组件、尺寸、文字和交互方式。
6. **渐进披露**：高频内容优先展示，低频和高级选项按需展开。
7. **可访问性**：不能只依靠颜色传达信息，应同时使用图标、文字或形状。
8. **可预测性**：组件行为、输入规则、错误处理和页面切换应保持一致。
9. **边界优先**：正常数据之外，必须提前设计超长文本、空状态、异常状态、极值、窗口变化等情况。

---

# 二、字体排版规范

## 2. 字体

Windows 桌面应用优先使用系统 UI 字体。

推荐：

| 类型 | 字体 |
|---|---|
| 英文 / 数字 | **Segoe UI Variable** |
| 简体中文 | **Microsoft YaHei UI（微软雅黑 UI）** |
| 其他语言或字符 | 使用系统字体 Fallback |

原则：

- 优先使用系统字体。
- 不应为了视觉效果随意引入多套字体。
- 同一界面最多使用一套主要 UI 字体体系。
- 中文、英文、数字混排时，应优先保证一致的字面高度与可读性。

## 3. 字号层级

建议建立有限且稳定的字号层级：

| 层级 | 字号 | 字重 | 用途 |
|---|---:|---|---|
| Display | 24px | 600 | 特殊场景的大标题 |
| Title | 20px | 600 | 页面标题 |
| Heading | 16px | 600 | 区域标题 |
| Body | 14px | 400 | 正文、主要内容 |
| Label | 13px | 400 | 表单标签、辅助字段 |
| Caption | 12px | 400 | 次要说明、提示文字 |

原则：

- 常规正文建议 **14px**。
- 辅助文字建议不低于 **12px**。
- 避免使用 10px、11px 作为常规 UI 字号。
- 不通过缩小字体来解决布局空间不足的问题。

## 4. 字重

主要使用：

```text
Regular      400
Semibold     600
```

标题、按钮、关键状态使用 600；普通正文、标签、输入内容、辅助说明使用 400。

## 5. 行高

| 字号 | 建议行高 |
|---:|---:|
| 12px | 18px |
| 13px | 20px |
| 14px | 20～22px |
| 16px | 24px |
| 20px | 28px |
| 24px | 32px |

---

# 三、高 DPI 与多分辨率规范

## 6. DPI 适配

桌面 UI 应使用逻辑尺寸设计，而不是围绕物理像素设计。

不要为不同缩放倍率分别设计一套字号。

应保持设计尺寸稳定，由系统和 UI 框架完成 DPI 映射。

## 7. 测试缩放

至少验证：

```text
100%
125%
150%
175%
200%
250%
```

重点检查：

- 文字是否截断
- 文字是否垂直居中
- 控件是否异常放大
- 控件是否出现拥挤
- 中文和英文混排是否错位
- 卡片高度是否合理
- 布局是否出现异常空白

## 8. 测试分辨率

建议至少覆盖：

```text
1920×1080
2560×1440
2560×1600
3840×2160
```

同时结合不同系统缩放比例进行测试。

---

# 四、整体页面布局规范

## 9. 桌面应用基础结构

推荐：

```text
┌─────────────────────────────────────────────┐
│                 Window Area                 │
├──────────────┬──────────────────────────────┤
│              │                              │
│   Navigation │          Content             │
│              │                              │
│   一级菜单    │                              │
│   一级菜单    │                              │
│   一级菜单    │                              │
│              │                              │
└──────────────┴──────────────────────────────┘
```

页面切换时，导航区域保持稳定，只更新内容区域。

## 10. 推荐尺寸

```text
侧边导航宽度：200～220px
内容区左右 Padding：24px
内容区上下 Padding：24px
主要区块间距：16～20px
卡片 Padding：16px
```

## 11. 内容宽度

内容区域不建议无限拉宽：

- 配置、表单、说明文字优先控制阅读宽度。
- 避免输入框和正文在超宽窗口下无限延伸。
- 大屏幕下可设置最大内容宽度。
- 允许需要全宽的列表、表格、日志等组件突破普通阅读宽度限制。

---

# 五、左侧导航栏规范

## 12. 导航结构

建议使用一级导航表达主要页面模块：

```text
◉  一级菜单
○  一级菜单
○  一级菜单
○  一级菜单
```

原则：

- 一级导航表达页面级功能。
- 不把具体表单字段做成侧边菜单。
- 导航层级不宜过深。
- 菜单名称应简短、明确、统一。

## 13. 导航尺寸

```text
侧栏宽度：200～220px
菜单项高度：40px
图标尺寸：18～20px
图标与文字间距：10～12px
左右 Padding：12px
菜单项之间：2～4px
菜单圆角：6～8px
```

## 14. 导航状态

至少支持：

```text
Normal
Hover
Pressed
Selected
Keyboard Focus
Disabled
```

### Selected

推荐：

```text
轻量强调背景
+
品牌色图标
+
Semibold 文字
```

避免使用大面积高饱和实心背景。

### Keyboard Focus

必须显示明确的 Focus Ring，不能只依赖 Hover。

## 15. 导航交互

点击后：

```text
显示目标页面
↓
更新 Selected
↓
原页面取消 Selected
```

原则：

- 重复点击当前页面不应导致无意义的完整重载。
- 页面切换建议立即切换或轻量淡入。
- 避免大幅滑动、缩放等强动画。

---

# 六、页面标题与信息层级

## 16. 页面标题

```text
20px / Semibold 600
```

标题下的辅助说明：

```text
12～13px / Regular 400
```

## 17. 信息优先级

```text
一级：页面目标 / 当前状态
二级：核心内容 / 关键数据
三级：解释 / 辅助信息
四级：高级选项 / 低频操作
```

---

# 七、表单输入规范

## 18. 表单总体结构

推荐：

```text
分组标题
   ↓
Label
   ↓
Input
   ↓
Description
   ↓
下一个字段
```

示意：

```text
字段名称
[ 输入内容                         ]
补充说明信息
```

## 19. 表单分组

按照语义分组，而不是按照控件类型分组。

推荐：

```text
基本设置
行为设置
高级设置
```

不推荐：

```text
输入框
下拉框
开关
按钮
```

## 20. 字段排列

默认采用垂直布局。

复杂页面可以使用双列布局，但必须保证：

- 标签和输入控件对齐。
- 左右列宽度稳定。
- 小窗口下可以退化为单列。
- 较长标签不会挤压输入空间。

## 21. 表单间距

```text
字段之间：16px
Label → Input：6～8px
Input → Description：4～6px
分组之间：20～24px
分组标题 → 第一字段：12～16px
```

## 22. Input 输入框

```text
高度：36px
字号：14px / Regular 400
圆角：6～8px
左右 Padding：10～12px
```

不建议使用过重的边框。

## 23. 输入状态

至少支持：

```text
Normal
Hover
Focus
Disabled
Error
Read-only
```

Focus：

- 使用品牌色边框或 Focus Ring。
- 不改变控件尺寸。
- 不导致周围布局跳动。

Error：

- 输入框错误强调。
- 提供明确错误说明。
- 不只通过颜色表达错误。

---

# 八、表单边界与极端内容规范

> 本章节是表单设计的强制检查项。开发完成前，应使用测试数据验证所有边界情况。

## 24. 超长 Label

当字段名称过长时，不允许无限横向撑开表单。

例如：

```text
是否在应用启动时自动恢复上一次保存的用户设置并在发生异常后重新应用
[ 开启 ]
```

推荐处理方式：

### 优先方案：换行

```text
是否在应用启动时自动恢复上一次保存的用户设置
并在发生异常后重新应用
[ 开启 ]
```

### 次选方案：截断 + Tooltip

```text
是否在应用启动时自动恢复上一次保存的…
```

鼠标 Hover 后显示完整内容。

原则：

- 不允许 Label 撑破布局。
- 不允许文字覆盖相邻控件。
- 不建议无理由缩小字号。
- 可根据字段复杂度适当增加垂直空间。

## 25. 超长单行输入内容

例如：

```text
https://example.com/very-long-path/aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
```

处理：

- 输入框不应横向撑大。
- 光标始终可见。
- 文本可以在水平方向滚动。
- 失去焦点后可根据产品需求显示尾部内容或起始内容。
- 不建议在输入框内部强制换行处理单行值。

## 26. 超长只读 Value

例如：

```text
配置名称
this-is-a-very-long-configuration-name-that-cannot-fit-in-the-normal-layout
```

推荐：

```text
this-is-a-very-long-configuration-name-th…
```

并提供：

- Tooltip
- 复制
- 查看完整内容

至少一种完整内容获取方式。

原则：

> 截断只能解决视觉空间问题，不能让用户失去访问完整数据的能力。

## 27. 超长普通正文

长说明文字应允许自然换行：

```text
这里是一段比较长的说明文字，用于解释某个设置的用途、影响范围、
使用条件以及可能产生的结果。文字可以在容器宽度内自动换行，
但不应该超出卡片或者覆盖其他控件。
```

推荐：

- 保持合理最大宽度。
- 允许自动换行。
- 行高保持稳定。
- 段落之间增加适当间距。

## 28. 超长连续字符串

重点测试：

```text
aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
123456789012345678901234567890123456789012345678901234567890123456
AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA
```

不能简单依赖普通的自动换行。

推荐：

- 长单词允许在必要位置断行。
- URL、Hash、Token、文件路径等可提供水平滚动或截断。
- 根据内容类型选择“换行”或“横向滚动”，不要一律处理。

## 29. 极长路径

例如：

```text
C:\Users\UserName\AppData\Local\Application\Configuration\Profiles\Production\Backup\VeryLongFileName.json
```

推荐：

```text
C:\Users\UserName\...\VeryLongFileName.json
```

优先保留有辨识度的信息，例如：

- 文件名
- 末级目录
- 协议或域名
- 关键前缀

同时支持 Tooltip 查看完整值。

## 30. 空输入

空输入不能表现为：

```text
-
```

然后让用户猜测含义。

根据场景区分：

```text
未设置
未填写
无数据
不可用
未知
```

不同语义必须保持一致。

## 31. 缺省值与 Placeholder

Placeholder：

```text
[ 请输入内容 ]
```

不能代替 Label。

如果字段已有默认值：

```text
字段名称
[ 默认值 ]
```

不要把默认值藏在 Placeholder 中。

## 32. 极短输入

需要兼容：

```text
A
1
0
-
.
```

不要假设所有输入至少有若干字符。

## 33. 最大长度

需要明确：

```text
允许最小长度
允许最大长度
字符类型
换行规则
是否允许空格
是否允许特殊字符
```

达到上限后：

- 输入不应无提示继续增加。
- 如果有字数限制，可以显示计数。
- 错误信息应说明限制。

例如：

```text
名称
[ 示例名称 ]
12 / 32
```

## 34. 输入错误状态的布局稳定性

错误信息出现后，不能导致页面中的其他字段发生异常跳动。

推荐：

```text
正常：

Label
Input
Description

错误：

Label
Input（错误）
Error Message
```

页面高度可以增加，但水平布局、控件宽度和重要操作位置应尽量稳定。

---

# 九、选择类控件规范

## 35. Radio / Segmented Control

适合互斥选项。

选项较少时，应直接显示所有选项。

## 36. Checkbox

适合：

- 可独立启用或关闭的设置
- 多项可以同时选择的选项

## 37. Switch

适合明确的 On / Off 状态。

不要用 Switch 表示需要同时填写复杂参数的功能。

## 38. Select / ComboBox

适合：

- 选项较多
- 选择项较长
- 需要节省页面空间

边界情况：

- 选项文本过长时截断。
- 打开下拉菜单后应可查看完整内容。
- 下拉菜单不能超出屏幕可视范围。
- 选项较少时避免使用层级过深的下拉菜单。

---

# 十、按钮规范

## 39. Button

```text
标准按钮高度：36px
紧凑按钮高度：32px
字号：14px / 600
圆角：6～8px
```

按钮层级：

```text
Primary
Secondary
Subtle
Destructive
```

同一个操作区域不应同时出现多个视觉权重相同的 Primary。

## 40. 超长按钮文字

例如：

```text
[ 保存当前所有设置并立即应用到系统 ]
```

处理方式：

- 优先缩短文案。
- 需要完整说明时，可使用 Tooltip 或次级说明。
- 不建议让按钮文字自动换成两行。
- 按钮宽度应有合理上限。

推荐文案：

```text
[ 保存并应用 ]
```

说明放在按钮之外。

## 41. 按钮过多

当操作超过 3～4 个时：

- 保留一个主要操作。
- 次要操作降级。
- 低频操作放入“更多”菜单。

不要让所有操作都呈现同样的视觉权重。

---

# 十一、表单展示规范

## 42. 只读信息

不可编辑的数据不应使用 Input 模拟。

推荐：

```text
Label
Value
```

## 43. Key-Value

推荐：

```text
项目名称        内容
项目状态        正常
更新时间        10:32
```

Label：

```text
13px / Regular
```

Value：

```text
14px / Regular
```

关键 Value：

```text
14px / Semibold
```

## 44. 超长 Value

推荐：

```text
项目名称
very-long-project-name-that-is-too-long-to-fit…
```

同时支持：

```text
Tooltip
复制
查看详情
```

不要强制撑开整个布局。

## 45. 多行 Value

适合说明、错误原因、长文本等：

```text
项目说明
这里是一段较长的说明文字，可以自然换行，
但不应覆盖下方内容。
```

默认允许换行，不建议使用固定高度导致内容被截断。

---

# 十二、状态与特殊状态展示

## 46. 状态

推荐：

```text
● 正常
● 已启用
○ 未运行
⚠ 注意
✕ 失败
```

状态由：

```text
图标 + 文案 + 颜色
```

共同表达。

## 47. 空状态

列表没有数据时，不要显示一个完全空白的页面。

推荐：

```text
暂无数据

这里暂时没有可显示的内容。
```

必要时提供：

```text
[ 创建 ]
[ 刷新 ]
```

等下一步操作。

## 48. Loading 状态

应明确告诉用户系统正在进行什么操作：

```text
正在加载…
正在保存…
正在同步…
正在处理…
```

避免只显示一个无限旋转图标。

## 49. Error 状态

推荐：

```text
发生了什么
+
可能原因
+
下一步操作
```

例如：

```text
加载失败

暂时无法读取数据。

请检查网络或稍后重试。

[ 重试 ]
```

## 50. Partial Data

如果部分数据成功、部分失败，不要整体显示为失败。

例如：

```text
已加载 8 项
其中 1 项读取失败

[ 查看失败项 ]
```

让用户区分：

```text
成功
部分成功
失败
```

---

# 十三、列表规范

## 51. 列表行

列表项高度应根据内容确定：

```text
单行信息：40～48px
双行信息：56～72px
```

避免所有列表无论内容多少都固定一个高度。

## 52. 超长列表项

例如：

```text
非常非常长的项目名称以及一段非常非常长的项目说明……
```

推荐：

- 标题最多显示 1～2 行。
- 超出后截断。
- Tooltip 或详情页提供完整内容。
- 保持列表行高稳定。

## 53. 空列表

不要只留下空白容器。

推荐：

```text
暂无记录
这里还没有内容
```

## 54. 大型列表

数据量较大时应考虑：

- 虚拟化
- 分页
- 搜索
- 筛选
- 排序

不要让数千行数据一次性撑开界面。

---

# 十四、表格规范

## 55. 表格列宽

列宽应根据内容类型设计：

- 时间类：固定较窄宽度
- 数值类：根据最大合理长度设定
- 描述类：弹性宽度
- 操作类：固定宽度

## 56. 超长单元格

默认：

```text
单行 + 省略号
```

示例：

```text
very-long-text-that-cannot-fit-in-table-c…
```

完整内容可通过：

```text
Tooltip
详情
复制
```

访问。

## 57. 多行表格

如果必须展示多行：

- 明确行高。
- 保持列对齐。
- 不让某一行异常撑高整个表格。
- 大文本可在详情面板中查看。

## 58. 表格没有数据

推荐：

```text
暂无数据
```

而不是只显示表头。

---

# 十五、通知与消息规范

## 59. Toast / Inline Message

成功、提示等轻量反馈优先使用：

```text
✓ 操作成功
```

不要为所有简单操作弹模态窗口。

## 60. 消息过长

错误消息或提示内容超过一行时：

- 允许换行。
- 设置合理最大宽度。
- 不让消息无限变宽。
- 关键操作按钮保持可见。

## 61. 高优先级警告

严重错误可以使用更明显的 Banner 或对话框。

但必须避免大量页面长期显示高强调色。

---

# 十六、Tooltip 与完整内容查看

## 62. Tooltip 使用原则

Tooltip 适合补充：

- 被截断的文本
- 不常用的术语
- 图标按钮含义
- 辅助说明

不应把核心操作说明完全藏在 Tooltip 中。

## 63. 长文本查看

对于较长内容，可提供：

```text
查看全部
复制
展开
详情
```

原则：

> 视觉上可以截断，但信息本身不能不可访问。

---

# 十七、滚动规范

## 64. 页面滚动

内容超出窗口时：

- 内容区域独立滚动。
- 导航栏保持稳定。
- 页面标题和关键操作可按需要固定。

## 65. 横向滚动

仅在这些场景考虑横向滚动：

- 表格
- 长路径
- 长代码
- 长单行值
- 技术日志

普通页面正文不建议依赖横向滚动。

## 66. 滚动与焦点

键盘 Tab 移动到屏幕外控件时，应自动滚动到可视区域。

---

# 十八、窗口尺寸与响应式

## 67. 小窗口

空间不足时，优先：

```text
双列 → 单列
横向操作 → 垂直操作
次要说明 → 折叠
低频操作 → 更多菜单
```

不要首先缩小字体。

## 68. 大窗口

空间充足时：

- 增加适度留白。
- 设置最大阅读宽度。
- 避免组件无限拉伸。
- 列表和表格可以利用更多横向空间。

## 69. 最小窗口

设计时必须明确：

```text
最小可用宽度
最小可用高度
```

达到最小窗口时：

- 主要操作仍可访问。
- 文字不重叠。
- 控件不被裁剪。
- 必要内容可滚动。

---

# 十九、国际化与文本膨胀

## 70. 文本长度不能写死

不同语言可能出现：

```text
中文：
保存

英文：
Save

德文等语言：
更长
```

因此：

- 按钮不应依赖固定文字宽度。
- Label 不应假定固定字符数量。
- 多语言环境下需要测试文本膨胀。

## 71. 数字与日期

不要假定所有数字长度一致。

测试：

```text
0
1
999
1000
999999
1,234,567.89
```

时间：

```text
00:00
12:34
23:59:59
```

日期格式应允许根据系统区域变化。

---

# 二十、交互反馈规范

## 72. Loading

执行耗时操作时：

```text
显示进行中状态
+
避免重复触发
+
尽可能保留当前上下文
```

## 73. Success

采用轻量反馈：

```text
✓ 已保存
```

## 74. Error

回答：

```text
发生了什么？
为什么发生？
下一步怎么办？
```

## 75. Confirmation

只有真正具有风险或难以撤销的操作需要确认。

确认内容应明确：

```text
操作是什么
可能造成什么结果
取消 / 确认分别是什么
```

---

# 二十一、动画与交互反馈

## 76. 动画原则

动画只用于：

- 状态变化
- 页面切换
- 操作反馈
- 视觉连续性

不要为了装饰增加复杂动画。

## 77. 动画时长

一般 UI 状态反馈建议：

```text
约 100～150ms
```

高频交互更应克制。

---

# 二十二、设计 Token

建议建立：

```text
Typography
Spacing
Radius
ControlSize
Color
Icon
Elevation
```

避免散落大量硬编码。

## 78. Spacing

```text
4px
8px
12px
16px
20px
24px
32px
```

## 79. Radius

```text
6px
8px
12px
```

## 80. Control

```text
32px
36px
40px
```

## 81. Icon

```text
16px
18px
20px
24px
```

---

# 二十三、边界测试数据清单

设计评审和开发联调时，建议至少使用以下数据测试：

## 文本

```text
空字符串
A
1
中文
English
中文 English 123
非常长的中文文本非常长的中文文本非常长的中文文本非常长的中文文本
This is a very long English sentence that should wrap correctly without breaking the layout.
aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
```

## 数字

```text
0
1
-1
999
1000
999999
999999999999
0.1
99999.99
```

## 特殊文本

```text
192.168.0.1
https://example.com/very/long/path
C:\Users\UserName\AppData\Local\Application\Configuration\VeryLongFileName.json
abcdef1234567890abcdef1234567890abcdef1234567890
```

## 状态

```text
正常
处理中
部分成功
失败
未知
无数据
禁用
```

---

# 二十四、设计完成检查清单

## 信息层级

```text
□ 页面目标一眼可见
□ 一级、二级、三级信息层级明确
□ 关键操作位置清楚
□ 辅助内容不会抢占主要视觉焦点
```

## 导航

```text
□ Normal / Hover / Pressed / Selected / Focus / Disabled 完整
□ 当前页面有明确 Selected
□ 菜单名称简短统一
□ 没有不必要的多级菜单
```

## 表单

```text
□ 每个输入项都有明确 Label
□ Placeholder 没有替代 Label
□ 输入与只读展示明显区分
□ Focus 状态明确
□ Error 状态明确
□ 保存 / 提交状态明确
□ 未保存状态明确
```

## 长文本

```text
□ 超长 Label 不撑破布局
□ 超长 Input 不撑破布局
□ 超长 Value 可访问完整内容
□ 长 URL / 路径有合理处理
□ 长连续字符串不会导致布局溢出
□ Tooltip / 详情 / 复制功能按需提供
```

## 边界状态

```text
□ 空状态
□ Loading
□ Error
□ Partial Data
□ Disabled
□ Read-only
□ 最大长度
□ 最小长度
□ 极大数字
□ 极短文本
□ 极长文本
```

## 响应式

```text
□ 100%
□ 125%
□ 150%
□ 175%
□ 200%
□ 250%

□ 小窗口
□ 普通窗口
□ 大窗口
□ 4K 高 DPI
```

---

# 二十五、最终速查表

## Typography

```text
Display       24px / 600
Title         20px / 600
Heading       16px / 600
Body          14px / 400
Label         13px / 400
Caption       12px / 400
```

## Navigation

```text
Sidebar       200～220px
Menu Item     40px
Icon          18～20px
Radius        6～8px
```

## Form

```text
Input         36px
Button        36px
Compact       32px
Label→Input   6～8px
Field Gap     16px
Group Gap     20～24px
Card Padding  16px
```

## Page

```text
Content Padding：24px
Section Gap：16～20px
Card Radius：8～12px
```

## 边界原则

```text
视觉空间不足 → 优先换行 / 截断 / 折叠
信息过长     → 提供 Tooltip / 详情 / 复制
内容为空     → 提供明确空状态
操作进行中   → 显示 Busy 状态
操作失败     → 说明原因和下一步
窗口变小     → 重排布局，不优先缩小文字
```

## 核心原则

```text
清晰优先
层级明确
紧凑但不拥挤
状态可见
组件一致
渐进披露
支持键盘
适配高 DPI
不依赖颜色表达语义
完整处理边界状态
```
